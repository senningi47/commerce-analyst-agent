# 消融 §17.2 执行报告：120/120 COMPLETE——修复价值 = 0（描述性），A 条件省 58–64% 成本（2026-09-17）

> 授权链：消融付费门呈批（cap 25 元）→ 用户「授权消融」→ B 条件（午窗）→ A 条件（晚窗）→ COMPLETE。
> 分批窗口：B 12:37–13:19（午间 off-peak）；A 18:03–18:39（晚窗 off-peak）——A 原可与 B 同窗，但估时贴 14:00 peak 线，按 off-peak-only rider 分窗。
> 实验身份：`ablation-repair-20260917-b` / `-a`（config-hash `e542a721…` = task-pool-30.jsonl sha256）；purpose `ablation_repair`。

## 1. 设计与实现

- **§17.2 条件**：A = 首次失败停止；B = 官方允许的有限修复（= A4 主运行默认行为）。30 配对任务 × c/a × A/B = 120 集；两条件独立干净任务库、互不可见 Trace。
- **条件 A 实现**（官方 ADK 无此旋钮，`e0b3844`）：c-mode = 失败提交后的官方 debug 轮在 adapter 入口零模型调用返回终止文本；a-mode = `_ConditionalStopGate` 在失败提交后的下一模型轮前拦截（`ablation_stop_on_submit_fail`）。默认关闭，`BIRD_ABLATION_CONDITION=a` 经 compose 白名单（`d70b429`）注入才激活；Full 式运行零行为变化。891 passed + rebuild + 容器实证。
- **选取**（`e87391b`）：seed 20260916，从 155 个 c/a 配对 A4 任务分层抽 30 题（30 库族各 1）；GT 拆分双模式已在盘，零新 GT 处理。
- **执行纪律修正**：60 集混合清单会触坑 63 守卫 → 拆 c/a 单模式清单串行（同题跨模式永不同批）。

## 2. 结果（eval 视图 + spool 聚合逐分一致）

| 格 | 集 | P1 | P2 | reward>0 | turns（每集） | submits/集 | agent $ |
|---|---|---|---|---|---|---|---|
| **B/c**（修复） | 30 | 0 | 0 | 0 | 125（4.2） | 1.87 | 0.2951 |
| **A/c**（停止） | 30 | 0 | 0 | 0 | 91（3.0） | **1.00** | 0.1075 |
| **B/a**（修复） | 30 | 0 | 0 | 0 | 319（10.6） | 2.27 | 0.8933 |
| **A/a**（停止） | 30 | 0 | 0 | 0 | 234（7.8） | **1.00** | 0.3774 |

- **修复救回 0 集**：P1 两条件完全相同（0/30 vs 0/30）——debug 反馈环在该子集上未产生任何一次通过的重新提交。§17.2 的描述性结论：**当前能力水平下修复价值 = 0**（失败主因 = 知识缺口 + SQL 质量，属修复不可达层）。
- **修复经济学为负**：B 条件多烧 2.4–2.7× 轮次与费用，P1 增益为零。
- **A 语义活体验证**：A 格恰 1.00 submit/集（B 1.87–2.27）；A/c 完成速度 ~2× B/c。
- infra 0；120/120 off_peak；`ContextBudgetExceeded` 集数 0。

## 3. 账目

- agent 合计 **$1.673449 ≈ 11.8 元**（cap 25 元内，余 ~13 元）；sim 侧待余额核对（估 ~$0.2）。
- telemetry：276 spool 记录 → 按 (experiment, task, mode) 预聚合 120 组 → **120/120 精确归属**。**新发现**：c 模式 spool 每模型轮一个文件，导入器 1 session=1 attempt 的假设对 c 格欠记 ~4×（首次合并 A/c 仅 $0.0262 vs 真值 $0.1075）——导入变体按集预聚合后修正（merge_telemetry 为 jsonb 顶层键替换，重跑安全）；DB 四格与 spool 聚合逐分一致。c 批（304 集）当年未做 DB 导入，不受影响。

## 4. 现场

栈与官方库已 stop（仅产品 PG）；eval 库新增 2 实验身份 120 attempt 行 + telemetry；spool +276 文件；episode 工件 120 份。commit：`e0b3844`（条件 A）、`d70b429`（compose 接线）、`e87391b`（选取工件）已入库；本报告与文档刷新待授权。
