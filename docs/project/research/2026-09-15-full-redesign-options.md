# Full 重设计方案对比（Day 6 Task 11，零付费分析，交用户裁定）

> 执行：Claude Code（GLM）· 2026-09-15（Asia/Shanghai）
> 授权链：Day 6 打包授权（决策按推荐行使、逐 Task commit）；本分析零付费、零 GT 读取、零新 API 调用——全部数字来自既有运行产物（Pilot d / Task 10 三次运行 / 账本）。
> **Full 启动与否由用户裁定**；本文件只产出方案对比与建议，不构成启动。
> 输入：`docs/reports/2026-09-14-task10-strategy-validation.md` §5/§8（修复后单价）、`docs/reports/2026-09-13-task13-pilot-report.md` §3.5/§8/§9（预算门终裁与 band 纪律）、`outputs/bird-budget/pilot-ledger.json`（gitignored，两节成本账）、规格 v0.3 §16.4/§17.3、Day 6 计划 Task 11 定义。

## 0. 结论（TL;DR）

1. **方向 0（现状范围 + 修复后单价）重推：基准情景 201.6 元 = 1.26× 越线（FAIL），乐观 147.9 元勉强过线（余 8%），保守 298.6 元（1.87×）。** 相比 Pilot 终裁的 457.6 元（2.86×），策略修复已把总账砍掉 ~56%——**能力门修复本身就是最大的降本杠杆**（c 集澄清 60→12 轮，sim 调用数 -80%）。
2. **修正建模披露（诚实声明）**：Pilot FAIL 的 sim 侧按 20 集总量均摊（$0.03144/集）对 1180 集平摊，对 a-mode 高估 ~8–10×。Task 10 数据使按调用数建模成为可能（sim ~$0.001/调用；a-mode 每集仅 ~3–4 次调用）。修正后**「a-mode Full 剩余单项 210 元超线」不再成立**（修正后 ~96 元），「Full 全量数学上不可行」软化为「1.26× 越线，温和杠杆可收敛」。这不回溯推翻 Pilot FAIL（当时数据下的保守裁定，判断纪律未变），但改变了今天的方案空间。
3. **三方向定性**：A（范围压缩）是唯一能同时满足两情景预算的杠杆；B（sim 降本）修复后价值大减（sim 已从 69% 占比降到 ~40%，且 deepseek-flash 可能已是平台价格地板）退化为放大器；C（上限修正）不改变成本结构、只移动判据线，且需修两处冻结规格。
4. **期望 reward 增益 = 0（三方向共同）**：A/B/C 全是成本杠杆，不动 SQL 质量。当前策略下 19/19 集 reward=0（Pilot 18 + Task 10 Run 3），**任何范围的 Full 现在启动都会产出全 0 分**。能力门尚无正向证据。
5. **建议（按推荐行使，裁定权在用户）**：① 现在不启动任何 Full 变体；② 若批准能力验证（2–4 集，~$0.05–0.10，需新授权）且 reward>0 达成 → 首选 **A4 = 300/模式分层减量 + sim 合规排查后择机启用**（基准 91.6 元 / 保守 135.6 元，两情景均过线且有余量，墙钟 ~15h）；③ 若用户优先官方 1200 集可比性 → 选 **C**（ceiling 260–320 元，修规格显式批准，基准 201.6 / 保守 298.6 元）；④ 能力验证有界失败 → A1 c-only 最小工件或搁置，再裁定。

## 1. 修复后单价锚点（Task 10 实测口径）

| 项 | 值 | 来源与口径 |
|---|---|---|
| c agent/集 | **$0.0041**（14 模型轮） | Run 3 实测（spool 逐轮聚合，**n=1**）；任务 = archeology_scan_M_4 |
| c sim/集 | **~$0.013**（12–14 调用 × ~$0.001） | Task 10 估算（sim 无 spool，权威数字待用户余额核对） |
| **c 合计/集** | **~$0.017** | Task 10 报告 §8 |
| a agent/集 | **$0.0136 mean / $0.0188 p95**（18 轮） | Pilot 9 集实测；v3 策略不改轮次（bird-coin 18 固定，先提交后迭代不省轮） |
| a sim/集 | **~$0.003–0.004**（init + ~1 ask + final ≈ 3–4 调用） | 新建模（§2）；Pilot a-mode ask_user 共 10 次/9 集 |
| sim 每调用 | **~$0.001** | 双锚：Pilot $0.6288 ÷ ~620–650 调用；Task 10 账本口径（~77 调用 × $0.00098） |
| a-mode 完整整集（v3 策略） | **未实测** | Run 1 仅 7 轮即被 infra 中断（$0.0115）——且 Run 1 跑在陈旧镜像上（v1 策略），不能作 v3 成本证据；只能作「大 schema 任务单价可偏高」的旁证 |

**轮次→成本模型（agent 侧，由 Pilot 60 轮 $0.0100 与 Run 3 14 轮 $0.0041 两点拟合）**：`agent/集 ≈ $0.0023 + $0.000128 × 轮数`（固定 prompt 占大头，边际轮便宜）；sim 侧 = `(ask 数 + ~2) × $0.001`。c 集澄清数受官方 `max_turn = n_critical + n_knowledge + patience`（~12–18）上界约束。

## 2. sim 成本模型修正（对 Pilot 终裁的影响，如实披露）

Pilot 报告 §3.5 的 sim 侧 = $0.6288 ÷ 20 集 = **$0.03144/集平摊**到全部 1180 集（报告自注「分布未知，偏保守」）。Task 10 的 per-call 数据揭示该平摊对 **a-mode 高估 ~8–10×**：a-mode 每集 sim 调用 ~3–4 次（≈$0.003–0.004），而非失控 c-mode 的 ~61 次（≈$0.061）。

| 项 | Pilot 平摊口径 | 修正口径（per-call × 每模式调用数） |
|---|---|---|
| Full 剩余 sim（1180 集） | $37.10 | **~$10.0**（590c×$0.013 + 590a×$0.004） |
| a-mode Full 剩余单项 | $29.64 ≈ 210 元（「单项超线」） | **~$13.5 ≈ 96 元**（不再单项越线） |
| 全账（现状范围） | 457.6 元 = 2.86× | **201.6 元 = 1.26×**（基准情景，§3） |

三点裁定口径声明：

1. **Pilot FAIL 不回溯推翻**——该裁定基于当时可得数据（sim 分布不可见）、取保守方向、且按当时单价 c 全量仍越线；「安全暂停交用户」的处置正确。
2. **结构性发现反转要如实入账**：「Full 全量数学上不可行」降级为「基准情景 1.26× 越线」。方案空间因此从「只能大砍」变为「温和杠杆可收敛」。
3. **修复即降本**：Pilot→修复后，c 集 $0.0414→$0.017（-59%）、sim 总盘 $37.1→$10.0（-73%）。Task 5/10 的策略修复同时服务于能力门与预算门。

## 3. 方向 0：现状范围 + 修复后单价（重推 §16.4）

**情景带**（每集合计 agent+sim；n 小，不做形式 bootstrap，以情景带代替 95% 上界——方法论偏离如实标注）：

| 模式 | 乐观 | 基准 | 保守 | 依据 |
|---|---|---|---|---|
| c | $0.012 | $0.017 | $0.025 | 澄清 ~5 / **12（Run 3 实测）** / ~18（max_turn 上界 + 大 schema 任务轮数上浮） |
| a | $0.017 | $0.023 | $0.035 | Pilot mean / **p95** / 大 schema 任务 + v3 envelope 情形（Run 1 旁证） |

**重推表**（spent = 平台 6.43 元 + Task 10 agent $0.025 + Task 10 sim 估 $0.05–0.08 + 探针 <$0.001 ≈ **$1.00 ≈ 7.1 元**，其中 Task 10 sim 侧待余额核对；汇率 7.07 指示性口径）：

| 分项 | 乐观 | 基准 | 保守 |
|---|---|---|---|
| Full 剩余 1180 集（590c+590a） | $17.11 | $23.60 | $35.40 |
| 产品 90 + 消融 120 集 | $2.82 | $3.93 | $5.85 |
| spent | $1.00 | $1.00 | $1.00 |
| **合计 USD** | **$20.92** | **$28.52** | **$42.24** |
| **合计（元 @7.07）** | **147.9** | **201.6** | **298.6** |
| vs 160 元线（180 ceiling − 20 保留） | ✓（余 12 元，8%） | **✗ 1.26×** | **✗ 1.87×** |

结构（基准情景）：a-mode Full 剩余 $13.57（≈96 元）+ c-side $10.03（≈71 元）+ 产品/消融 $3.93（≈28 元）+ spent 7 元。**乐观情景余量仅 8%，不足以给 1200 集运行作授权依据（c 锚点 n=1、a-mode v3 整集 n=0）。**

范围口径注：Task 10 的 1 题（archeology_scan_M_4）不在 Pilot 20 题内但属验证实验、不计入 Full 进度；保守起见仍按 1180 集外推（误差 1 集，噪声级）。

## 4. 方向 A：范围压缩（规格 §17.3 = 600×2 冻结，偏离 = 用户修规格批准）

| 子选项 | 剩余范围 | Full 剩余（基准/保守） | 总账（基准/保守，元） | vs 160 | 墙钟（÷2 并发） |
|---|---|---|---|---|---|
| **A1 c-only 600** | 590c + 0a | $10.03 / $14.75 | **105.7 / 152.6** | ✓ / ✓（余 5%） | ~10h |
| **A2 减量 200/模式** | 190c+190a | $7.60 / $11.40 | **88.5 / 129.0** | ✓ / ✓（余 19%） | ~10.5h |
| **A3 减量 300/模式** | 290c+290a | $11.60 / $17.40 | **116.8 / 171.4** | ✓ / ✗（1.07×） | ~15h |
| **A4 = A3 + B（sim −50%）** | 290c+290a | $9.14 / $13.92 | **91.6 / 135.6** | ✓ / ✓（余 15%） | ~15h |

- **抽样方案**（A2/A3 通用）：数据库 × 歧义度 × 操作类型分层、seed 固定（§16.3 fallback 先例——官方 manifest 无 BI/DM 标签）；结论一律标注「减量分层子样本，非官方 Full 分数」。
- **规格代价**：§17.3「Full 600 task ID 分别跑 c/a」与 §DoD「c/a 各 600 episode」不再满足，最终报告须声明缩围——这不是坑 31 的「为预算偷偷减任务」（偷减 = 隐瞒；此处 = 显式修规格 + 如实标注），但**必须走用户修规格**。
- **研究价值代价**：A1 丢弃 a-mode 全部证据——而 §17.3 的研究问题恰恰是 c/a 质量–成本差异（a-mode 是 benchmark 的自变量之一）；A2/A3 保留两模式但统计功效下降（任务级 paired difference 的 CI 变宽；200/300 对的 bootstrap 区间仍可支撑描述性结论）。
- 墙钟估算基础：c ~120s/集（Run 3 118s 实测）、a ~240s/集（估，Pilot a 集未逐项记录）。

## 5. 方向 B：sim 降本（修复后已退化为放大器）

1. **量化价值**：修复后 sim 总盘 ≈ $12.2 ≈ 86 元（c 590×$0.013 + a 590×$0.004 + 产品/消融 ~$2.2）。半价模型省 ~43 元；sim 全免也只省 ~86 元。对比修复前（sim 占 69%、总盘 $37+），**策略修复已经替 B 干完了大部分活**。
2. **B 单独（现状范围 + sim −50%）**：基准 158.4 元 ✓（余 1%，形同贴线）/ 保守 237.4 元 ✗。**结论：B 单独不足，只作 A/C 的放大器。**
3. **合规前提（开放项）**：规格 line 506 固定的是官方 simulator **软件**（不许重写 Evaluator/sim）；line 550 明示 sim 与 agent 同基模型「是预算和供应商约束下的选择」、公平性 = c/a 用**相同 sim 配置**。→ 规格内换模型可行，但 **Task 1 未覆盖官方规程对 sim 模型的约束**（排查过其笔记无此结论），需一次零付费静态排查（官方 repo sim 模型配置面 + 我方 sim 容器当前钉的 deepseek-flash）方可落地。**此项未做前 B 不可执行。**
4. **可行性关键在用户**：deepseek-flash 很可能已是平台最低价档（off-peak ¥0.02/M input 为价目表最低行）；若无更便宜模型则 B ≈ 0。零模型变更的替代杠杆 = context caching（sim 对话前缀天然稳定，收益未知，同属静态排查）。
5. **reward 侧风险**：换 sim 模型改变「用户」的答题质量 → 环境难度本身漂移 → 与 Pilot/官方任何参照都不可比；若启用，须与主运行解耦评估。

## 6. 方向 C：上限修正（规格冻结值，修改 = 用户显式修规格）

| 新 ceiling | 覆盖 | 对应情景 |
|---|---|---|
| 260 元（线 240） | 基准 201.6 +29% | 保守 298.6 仍 FAIL |
| 320 元（线 300） | 保守 298.6 +7% | 全情景覆盖（余 7% 偏薄） |

- **规格改动面**：§16.4「≤180 元」+ 规格总表「外部新增费用总计不超过 200 元，目标 100–200 元」两处冻结值。320 元 = 原始 200 元上限的 1.6×。
- **C 不改变任何成本结构**，只移动判据线；换来的是**官方 1200 集全量可比性**（DoD 原样达成）与 ~30h 墙钟（现状范围）。
- **C 不防纪律失败**：任何窗口滑入 peak（工作日 09–12/14–18）→ 剩余 ×2 → 340–520 元。**off-peak-only 是所有选项的强制 rider**，与是否修 ceiling 无关。

## 7. 期望 reward 增益假设（显式不确定性标注）

1. **A/B/C 的 reward 增益 = 0**（三者都是成本/范围杠杆）。当前策略下逐集 reward 期望 ≈ 0：**19/19 集 reward=0**（Pilot 18 有效集 + Task 10 Run 3），Run 3 两次提交均「Phase 1 测试用例未过」。**现在启动任何范围的 Full，最可能产出 = 全 0 分 + 通道贯通的工件。**
2. **能力杠杆独立存在于 A/B/C 之外**：① 知识检索利用（get_knowledge_definition 的定义是否进 SQL）；② 错误反馈修复（§17.2 B 条件机制，官方允许反馈）；③ a-mode「先提交一版再迭代」整集行为未验证。reward>0 是否可达 = **未验证假设，无任何实测支撑**，置信区间只能给「未知」。
3. **最便宜的信息单价 = 能力验证**：2–4 集（~$0.05–0.10，需新授权）即可把「reward>0 可达性」从未知变为已测。每 元 信息量远高于任何 Full 变体的首 25 集。
4. **决策树**：能力验证 PASS（出现 reward>0）→ 按 §8 启动所选 Full 变体；有界尝试（建议 ≤2 轮诊断循环）后仍全 0 → Full 任何范围都只产工件不产分数 → 决策变为「工件价值 vs 预算」（A1 c-only 最小工件运行，或搁置等策略再迭代）。
5. B 有额外 reward 侧风险（§5.5）；A 减样本只放大聚合 CI，不改变逐集分布；C 无影响。

## 8. 建议（按推荐行使的推荐；裁定权在用户）

**推荐序列**（也即 Task 11 呈报的裁定建议）：

1. **现在：不启动任何 Full 变体**（能力门无正向证据，方向 0 基准情景仍越线）。
2. **第一优先（若用户批准，需新付费授权 ~$0.05–0.10）**：能力验证 2–4 集（c/a 各半、新 experiment、off-peak、镜像已含 Task 5 修复），判据 = **reward>0 首分**。这是 Full 前置门，不是 Full 的一部分。
3. **能力验证 PASS 后的 Full 选型推荐：A4**（300/模式分层减量 + sim 合规排查通过后择机启用半价 sim）——唯一在基准与保守两情景下都 ≤160 线且余量 ≥15% 的选项；墙钟 ~15h；最终报告如实标注缩围子样本。
4. **若用户裁定官方全量可比性优先**：选 C（ceiling → 320 元保守覆盖 / 260 元仅覆盖基准情景），接受修两处冻结规格与保守情景余量仅 7%。
5. **能力验证有界失败**：回到用户裁定 A1（c-only 最小工件）或搁置。
6. **所有选项强制 riders**：① off-peak-only 调度（peak = 2×，账本 908.8 元惩罚口径）；② 每 25 集重估（§16.4 原文纪律）；③ Task 10 三修复项（同题双模式串行 / 代码变更后镜像 rebuild + 容器内实证 / `BIRD_EXPERIMENT_ID` 每实验注入）先入 Day 7 计划再启动；④ 任何 Full 首个 a-mode 集兼作 v3 整集成本验证，超保守带（$0.035/集）即安全暂停重估。

## 9. 裁定清单（交用户逐项裁定）

| # | 事项 | 选项 |
|---|---|---|
| ① | 是否批准能力验证（reward>0 门）作为 Full 前置 | 批准（~$0.05–0.10 新授权）/ 不批准（直接选 Full 方案或搁置） |
| ② | Full 范围 | A1 c-only / A2 200/模式 / A3 300/模式 / **A4 = A3+B（推荐）** / C 全量 / 搁置 |
| ③ | 若选 C：新 ceiling | 260 / 320 / 其他（= 显式修 §16.4 + 总预算两处规格） |
| ④ | sim 模型静态排查（B 前提）是否纳入 Day 7 | 纳入 / 跳过（B 作废） |
| ⑤ | Task 10 sim 侧余额核对 | 用户读平台当夜 deepseek-flash 消费 → 回填账本权威数字（也校准 §1 sim 单价锚） |

## 10. 口径与复现附注

- 所有单价锚点来源：§1 表逐行标注（Run 3 spool 实测 / Pilot 9 集实测 / Task 10 账本估算口径）。
- 情景带代替形式 bootstrap 的原因：c 修复后 n=1（Run 3）、a-mode v3 整集 n=0；样本不足以重采样。方法论偏离 §16.4「bootstrap 95% 上界」原文，以三情景带 + 保守情景作上界代理，已如实标注。
- 汇率 7.07 指示性口径（账本主口径）；平台 CNY 列 = 6.6667 × 快照 USD 列，按后者总账低 ~6%，不影响任何 PASS/FAIL 定性。
- spent $1.00 中 Task 10 sim 侧 $0.05–0.08 为估算（sim 无 spool），权威数字待裁定清单 ⑤。
- 墙钟为粗估（c 120s 实测 / a 240s 估），仅用于方案量级比较。
