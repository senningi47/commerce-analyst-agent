# Container-only runtime dependencies (spec v0.3 §21: the bird system agent
# keeps its own dependency set, outside the product lockfile). The pure-ASGI
# server needs only uvicorn — no FastAPI in the product venv.
#
# FROZEN at the Task 12 preflight (2026-09-12) via `uv pip compile` on the
# build machine (CPython 3.11); replace only with a new red-green change.
# RE-FROZEN at Task 13 pre-run (2026-09-13): the Task 12 freeze covered only
# the uvicorn chain, and the first real `compose up` failed with
# ModuleNotFoundError (pydantic) — build success is not runtime readiness.
# Input: the six top-level third-party imports of the image code
# (httpx/langgraph/psycopg/pydantic/sqlglot/uvicorn) pinned to the
# product-venv tested versions; compiled via `uv pip compile` (CPython 3.11).
# psycopg uses the [binary] extra: bare psycopg has no pq wrapper at import
# time (second live `compose up` failure, 2026-09-13), and the image has no
# system libpq.
annotated-types==0.8.0
anyio==4.15.1
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
distro==1.9.0
h11==0.16.0
httpcore==1.0.9
httpcore2==2.12.0
httpx==0.28.1
httpx2==2.12.0
idna==3.19
jsonpatch==1.33
jsonpointer==3.1.1
langchain-core==1.6.3
langchain-protocol==0.0.19
langgraph==1.2.11
langgraph-checkpoint==4.2.0
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.4
langsmith==0.12.4
orjson==3.12.0
ormsgpack==1.12.2
packaging==26.3
psycopg==3.3.4
psycopg-binary==3.3.4
pydantic==2.13.5
pydantic-core==2.46.5
pyyaml==6.0.3
requests==2.34.2
requests-toolbelt==1.0.0
sniffio==1.3.1
sqlglot==30.17.0
tenacity==9.1.4
truststore==0.10.4
typing-extensions==4.16.0
typing-inspection==0.4.4
tzdata==2026.4
urllib3==2.7.0
uuid-utils==0.17.1
uvicorn==0.52.4
websockets==16.1.1
xxhash==4.0.1
zstandard==0.25.0
